import Resource, { IResource } from '../models/resource';
import { ICourse } from '../models/course';
import { IStudent } from '../models/student';

export const createResource = async (course: ICourse, student: IStudent) => {
  console.log('Hi');
};
